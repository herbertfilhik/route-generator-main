pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Comandos para construir a aplicação
            }
        }
        stage('Test') {
            steps {
                // Comandos para executar testes
            }
        }
        stage('Deploy') {
            steps {
                // Comandos para deploy
            }
        }
    }
}
